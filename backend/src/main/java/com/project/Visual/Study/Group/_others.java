package com.project.Visual.Study.Group;

//Access Token: Short-lived JWT (e.g., 15 mins or 1 hour).
//Refresh Token: Long-lived secure token (stored securely in DB or Redis).
//When access token expires: The client uses refresh token to get a new access token (without re-logging in).
